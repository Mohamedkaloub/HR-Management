@model IEnumerable<Salary>
 <nav class="navbar navbar-light bg-light" dir="rtl">
  <form class="form-inline">
    <button class="btn btn-sm btn-outline-secondary" type="button"> <a class="dropdown-item" asp-action="ViewOne" asp-controller="Employee" asp-route-Id=@ViewBag.Id>الموظف</a></button>
    <button class="btn  btn-outline-success" type="button"><a class="dropdown-item" asp-action="index" asp-controller="Salary" asp-route-Id=@ViewBag.Id >المرتب</a></button>
        <button class="btn btn-sm btn-outline-secondary" type="button"><a class="dropdown-item" asp-action="index" asp-controller="Attendance" asp-route-Id=@ViewBag.Id>الاجازات</a></button>  
</form>
</nav>
<h1>سجل المرتبات</h1>
<div class="container ">
    <div class="row mb-3"><span class="col-10" ></span><span class="col-2"><a id="AddSalary" class="btn btn-success col-10 ">اضافة</a>
</span></div>
</div>
<table class="table table-borderd table-striped" id="Salarytable" dir="rtl">
  <thead>
    <tr>
      <th class="text-center">المرتب</th>
      <th class="text-center">الخصم</th>
      <th class="text-center">النهائي</th>
      <th class="text-center">التاريخ</th>
       <th class="text-center">العملبات</th>
  </tr>
  </thead>
 @* <tbody>

       @foreach(var x in Model){
            <tr>
      <td>@x.Total</td>
      <td>@x.punishment</td>
    <td><a class="btn-primary btn" asp-route-id="@x.Id" asp-area="" asp-controller="Salary" asp-action="Delete"/>مسح<a/></td>
     <td><a class="btn-primary btn" asp-route-id="@x.Id"  asp-area="" asp-controller="Salary" asp-action="Edit">تعديل</a></td>
    </tr>
       }
  </tbody>*@
</table>
<div class="modal fade modal" id="SalaryModal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="text-info modal-title">اضافة</h1>
                <button class="btn-close text-danger" data-bs-dismiss="modal" dir="ltr"></button>
            </div>
            <div class="modal-body" dir="rtl">
                <form method="post">
                    <input type="number" value="@ViewBag.Id" id="empid" hidden/>
               <div class="form-group"  >
      <label  class="form-label mt-10">المرتب </label>
      <input  class="form-control" type="number"  placeholder="المرتب" id="salary">
    </div>
     <div class="form-group">
      <label  class="form-label mt-10 ">الجزاءات </label>
      <input  type="number" class="form-control"   placeholder="الجزاءات" id="pun">
    </div>
    </form>
            </div>
            <div class="modal-footer">
                <button class="btn btn-primary" type="submit" id="SaveSalary">حفظ</button>
                <button class="btn btn-danger btn-default" data-bs-dismiss="modal" id="close">غلق</button>
            </div>
        </div>
    </div>
</div>

@section Scripts{
<script src="~/js/Salary.js" ></script> 
}
